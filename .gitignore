# Secrets
.env

# Personal compose overrides
docker-compose.override.yml

# CA certificates
*.pem
ca-bundle.*

# OpenCode runtime
opencode.json
auth.json
opencode.db
memory.json

# Docker-created mount target
opencode-docker/

# mkcert CA root (generated at first TLS startup)
.docker/mkcert-ca/

# RBI bridge state (macOS only)
.docker/rbi-bridge.pid
.docker/rbi-bridge.log
.docker/rbi-bridge-server.py

# OS
.DS_Store

.opencode
logs
.vscode
.claude
codebox/
